const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				display: ['Inter', 'Montserrat', '-apple-system', 'BlinkMacSystemFont', 'sans-serif']
			}
		}
	},

	plugins: []
};

module.exports = config;
